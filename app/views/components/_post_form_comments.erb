<%= form_for([locals[:tweet], @comment]) do |f| %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content, autofocus: true %> <%= f.submit "Add Comment" %>
  </div>


<% end %>
